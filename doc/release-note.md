Resource profiles
-----------------

- A new `-profile=<name>` startup option applies predefined CPU, disk, memory,
  and network defaults for the `pruned-tiny`, `pruned-low`, `performance`, and
  `server` profiles. Explicitly configured options take precedence over profile
  defaults.
